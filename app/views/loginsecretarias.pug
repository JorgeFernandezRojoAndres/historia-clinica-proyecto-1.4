extends layout

block content
  div.login-container
    div.form-container
      h2 Iniciar Sesión Secretaria o Administrador

      // Mensaje de error (si existe)
      if error
        div.alert.alert-danger #{error}

      form(action="/login/secretaria" method="POST")
        div.form-group
          label(for="username") Nombre de usuario
          input(type="text" id="username" name="username" placeholder="Nombre de usuario" required)
        div.form-group
          label(for="password") Contraseña
          input.contraLogin(type="password" id="password" name="password" placeholder="Contraseña" required)
        
        // Botón de enviar
        div
          button(type="submit") Entrar
      
    // Contenedor de imagen
    div.image-container
      img(src="/images/ImgLogSecr.jpg" alt="Login Secretaria")
